package pluginproject.pandawee.Mobs;

import org.bukkit.entity.LivingEntity;

public class CustomCreeper extends Mob {
    public CustomCreeper(LivingEntity entity, String name, int strength, int health, int moveSpeed) {
        super(entity, name, strength, health, moveSpeed);
    }
}
